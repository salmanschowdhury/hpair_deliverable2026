import { ArrowUpRight, Globe2, Sparkles } from 'lucide-react';
export default function Passport({ data, progress }) {
  const initials = `${data.firstName.trim()[0] || ''}${data.lastName.trim()[0] || ''}`;
  return <aside className="passport-panel" aria-label="Live profile preview"><div className="preview-label"><span className="live-dot" />YOUR PASSPORT, TAKING SHAPE<Sparkles size={14}/></div><div className="passport-card"><div className="passport-top"><span>HPAIR</span><Globe2 size={25} strokeWidth={1.2}/></div><div className="passport-watermark" aria-hidden="true"><Globe2 /></div><div className="avatar">{initials || <span>H</span>}</div><p className="passport-role">A NEW PERSPECTIVE</p><h3>{data.firstName.trim() || data.lastName.trim() ? `${data.firstName} ${data.lastName}` : 'Your name goes here'}</h3><p className="passport-school">{data.institution || 'Your journey is just beginning'}</p><div className="passport-meta"><div><span>ROOTED IN</span><strong>{data.nationality || 'A world of possibility'}</strong></div><div><span>CONNECTED THROUGH</span><strong>{data.language || 'Shared ideas'}</strong></div></div><div className="passport-bottom"><span>DELEGATE PROFILE</span><span>2026 — 27 <ArrowUpRight size={14}/></span></div></div><div className="completion"><div><span>Profile completeness</span><strong>{progress}%</strong></div><div className="progress-track" role="progressbar" aria-label="Profile completeness" aria-valuenow={progress} aria-valuemin={0} aria-valuemax={100}><span style={{width: `${progress}%`}} /></div><p>A few details. A fuller picture of you.</p></div><blockquote>“The best ideas begin<br/>with different perspectives.”</blockquote><p className="preview-footnote">YOURS IS ONE OF THEM.</p></aside>;
}
