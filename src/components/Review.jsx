import { Pencil, FileText } from 'lucide-react';
export default function Review({ data, file, edit }) {
  const groups = [{title:'About you',rows:[['Full name',`${data.firstName} ${data.lastName}`],['Email',data.email],['Nationality',data.nationality],['Preferred language',data.language]]},{title:'Your world',rows:[['Phone',data.phone],['Address',[data.address,data.city,data.region,data.postalCode,data.country].filter(Boolean).join(', ')]]},{title:'Your story',rows:[['University / organization',data.institution],['LinkedIn',data.hasLinkedIn ? data.linkedIn : 'Not provided'],['Interests',data.interests.join(', ')],['Introduction',data.introduction]]}];
  return <div className="review-groups">{groups.map((group,index) => <section className="review-group" key={group.title}><header><h3>{group.title}</h3><button type="button" className="text-button" onClick={() => edit(index)} aria-label={`Edit ${group.title.toLowerCase()}`}><Pencil size={13}/>Edit</button></header><dl>{group.rows.map(([label,value]) => <div key={label}><dt>{label}</dt><dd>{value || 'Not provided'}</dd></div>)}</dl>{index === 2 && <div className="review-file"><FileText size={18}/><span>{file?.name || 'CV needed'}</span><span>{file ? `${Math.ceil(file.size / 1024)} KB` : ''}</span></div>}</section>)}</div>;
}
